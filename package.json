{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "professional homepage",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass:compile": "node-sass src/style.scss -o src/ --output-style compressed",
    "watch:sass": "npm run sass:compile -- --watch"
  
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "node-sass": "^9.0.0"
  }
}
